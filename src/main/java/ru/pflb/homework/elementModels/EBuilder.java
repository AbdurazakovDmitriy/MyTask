package ru.pflb.homework.elementModels;

import org.openqa.selenium.By;
import ru.pflb.homework.config.DriverManager;

public class EBuilder {
    private void scanElement(){

//        new Button (DriverManager.get(null).findElement(By.xpath("")).getAttribute("onClick"));
    }
}
